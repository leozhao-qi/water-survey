export const SET_LESSONS = (state, lessons) => state.lessons = lessons

export const SET_LESSON = (state, lesson) => state.lesson = lesson

export const SET_OBJECTIVE = (state, objective) => state.objective = objective